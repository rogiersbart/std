# .onAttach <- function(libname, pkgname) {
#   err("! Package {{{.yellow('std')}}} is still in its experimental lifecycle stage.")
#   err("! Use at your own risk, and submit issues here:")
#   err("! {.link('https://github.com/rogiersbart/std/issues')}")
#   err("i You are attaching the {{{.yellow('std')}}} namespace.")
#   err("i We advise using {.link('std::err()', 'ide:help:std::err')} instead!")
#   invisible()
# }
